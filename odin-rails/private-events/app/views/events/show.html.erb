<h1><%= @event.name %></h1>

<h4><%= @event.creator.name %></h4>

<p><%= @event.description %></p>

<ul class="attendee-list">
	<% @event.attendees.each do |attendee| %>
		<li><%= link_to attendee.name, attendee %> is attending this event </li>
	<% end %>
</ul>