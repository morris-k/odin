<h1><%= @event.name %></h1>

<h4><%= @event.creator.name %></h4>

<p><%= @event.description %></p>

<ul class="attendee-list">
	<% @event.attendees.each do |attendee| %>
		<li><%= link_to user_name(attendee), attendee %> <%= attend_tense(@event, attendee) %> this event </li>
	<% end %>
</ul>