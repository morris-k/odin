<% if current_user.likes?(post) %>
	<%= link_to "Unlike", likes_path(post_id: post.id), method: :delete, remote: true %>
<% else %>
	<%= link_to "Like", likes_path(post_id: post.id), method: :post, remote: true %>
<% end %>