var subjCont = $("div[data-<%= j(@subject.class.to_s.downcase)%>=<%= escape_javascript(@subject.id.to_s) %>]");
subjCont.find(".likes-count").first().html("<%= escape_javascript(render 'likes_count', subject: @subject) %>");
subjCont.find(".add-like").first().html("<%= escape_javascript(render 'like_unlike', subject: @subject) %>");